const numToText = (number) => {
    if (number === 0){
        return "First";
    }
    else if (number === 1) {
        return "Second";
    }
    else if (number === 2) {
        return "Third";
    }
};

export default numToText;